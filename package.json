{
  "name": "rewrite",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build:markdown": "esbuild js/markdown.js --bundle --format=esm --outfile=js/markdown.bundle.js --platform=browser",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Retrograde WebUI rewrite. Static deploy uses js/markdown.bundle.js and css/highlight-github-dark.min.css (no node_modules on the server). After editing js/markdown.js run: npm run build:markdown",
  "dependencies": {
    "dompurify": "^3.4.1",
    "highlight.js": "^11.11.1",
    "marked": "^18.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
